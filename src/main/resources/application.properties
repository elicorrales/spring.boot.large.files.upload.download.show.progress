server.tomcat.basedir=/home/devchu/Development/Client.Server/eclipse.workspace/spring.boot.large.files.upload.show.progress/temp
#the above results in (in System.properties):
#"catalina.home"


# enable multipart uploads
#spring.servlet.multipart.enabled=true
spring.servlet.multipart.enabled=false

# threshold after files are written to disk
#spring.servlet.multipart.file-size-threshold=100B

# max file size
#spring.servlet.multipart.max-file-size=3300MB
#spring.servlet.multipart.max-request-size=3315MB

## file storage properties
# all uploaded files through REST api will be stored here:
#this property's prefix has to match what's configured in FileStorageProperties
# the 'upload-dir' portion is converted into 'uploadDir' and that's the expected name
# inside the POJO (FileStorageProperties)
file.upload-dir=./uploads

